- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- create:
    - ~/AppData/Roaming/lsd
    - ~/.local

- link:
    ~/.bashrc:
        force: true
        path: bash/git_bash/bashrc.sh
    ~/.bash_profile:
        force: true
        path: bash/git_bash/bash_profile.sh
    ~/.gitconfig:
        path: git/win_gitconfig
    ~/.gitignore: git/gitignore
    ~/.idlerc: python/idlerc
    ~/.irbrc: ruby/irbrc
    ~/.minttyrc: mintty/minttyrc
    ~/.npmrc: node/npmrc
    ~/vimfiles: vim/vimfiles
    ~/_gvimrc: vim/gvimrc
    ~/_vimrc: vim/vimrc_win
    ~/_vsvimrc: vim/vsvimrc
    ~/.zshrc:
        force: true
        path: zsh/zshrc_win_git_bash.zsh
    ~/AppData/Roaming/lsd/config.yaml:
        create: true
        force: true
        path: lsd/windows_config.yaml

- shell:
    - [ test -f ~/.wslconfig || cp -f wsl/.wslconfig ~/ ]
